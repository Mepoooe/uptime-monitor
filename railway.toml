[build]
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "php artisan migrate --force && php artisan optimize && supervisord -n -c /etc/supervisor/conf.d/supervisord.conf"
healthcheckPath = "/up"
healthcheckTimeout = 60