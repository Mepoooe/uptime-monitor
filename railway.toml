[build]
dockerfilePath = "docker/php/Dockerfile"

[deploy]
startCommand = "php artisan migrate --force && supervisord -n -c /etc/supervisor/conf.d/supervisord.conf"
healthcheckPath = "/up"
healthcheckTimeout = 60